<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.onboarding.recruit.mapper.ApplicantMapper">
	<insert id="insert"
		parameterType="com.onboarding.recruit.domain.ApplicantVO"
		useGeneratedKeys="true" keyProperty="uid">
		insert into TBL_APPLICANT(uname)
		values(#{uname})
	</insert>
</mapper>